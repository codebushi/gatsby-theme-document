const config = {
  sidebar: {
    forcedNavOrder: ['/introduction', '/codeblock'],
    links: [{ text: 'Hasura', link: 'https://hasura.io' }],
    frontline: false,
    ignoreIndex: true
  }
};

module.exports = config;
